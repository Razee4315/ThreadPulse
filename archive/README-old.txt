ThreadPulse

1. Extract this ZIP.
2. Make sure Node.js 18+ is installed.
3. Open a terminal in the extracted folder.
4. Run:
   node threadpulse.mjs
5. Open:
   http://localhost:8787

The local Node server proxies requests to TypeSafe so the browser does not hit the CORS error.
